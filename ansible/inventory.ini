# Inventory - the machines Ansible manages.
# [servers] is a group name referenced by "hosts: servers" in the playbook.

[servers]
devops-vm ansible_host=192.168.56.50

[servers:vars]
ansible_user=devops
ansible_ssh_private_key_file=~/.ssh/id_ed25519
ansible_python_interpreter=/usr/bin/python3
